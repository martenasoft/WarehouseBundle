<form method="post" action="{{ path('shelving_delete', {'id': shelving.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shelving.id) }}">
    <button class="btn">Delete</button>
</form>
