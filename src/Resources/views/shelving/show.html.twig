{% extends 'base.html.twig' %}

{% block title %}Shelving{% endblock %}

{% block body %}
    <h1>Shelving</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shelving.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shelving.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shelving.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shelving_index') }}">back to list</a>

    <a href="{{ path('shelving_edit', {'id': shelving.id}) }}">edit</a>

    {{ include('shelving/_delete_form.html.twig') }}
{% endblock %}
